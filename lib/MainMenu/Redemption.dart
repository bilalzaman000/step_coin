// redemption_page.dart
import 'package:flutter/material.dart';

class RedemptionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
      body: Center(
        child: Text(
          'Redemption Page',
          style: TextStyle(color: Colors.white),
        ),
      ),
    );
  }
}
